import HeroBrandVisual from "@/components/marketing/HeroBrandVisual";

/** @deprecated Use HeroBrandVisual — hero no longer shows product data. */
export default function HeroVisual() {
  return <HeroBrandVisual />;
}
